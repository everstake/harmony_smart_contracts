{
  "name": "harmony-erc20",
  "version": "1.0.0",
  "description": "Harmony sample ERC20 deploy",
  "main": "truffle.js",
  "scripts": {
    "create-env": "cp .env-template .env",
    "cmp-harmony": "rm -rf build && npx truffle compile --network testnet",
    "comp-mig-harmony": "npx truffle compile && npx truffle migrate --reset --network testnet",
    "test-contracts-harmony": "npx truffle test --network testnet",
    "cmp-mig-ganache": "rm -rf build && npx truffle compile && npx truffle migrate --reset",
    "tests-in-ganache": "npx truffle test",
    "gan-cli": "npx ganache-cli -m 'puzzle crazy electric wide verify popular struggle latin genuine lawsuit thrive neck'",
    "sol-cover": "npx truffle run coverage"
  },
  "dependencies": {
    "@harmony-js/core": "^0.1.55",
    "@openzeppelin/contracts": "3.2.0",
    "dotenv": "^8.2.0",
    "js-sha256": "^0.9.0",
    "keccak256": "^1.0.0",
    "nano-ethereum-signer": "^0.1.2",
    "openzeppelin-solidity": "^3.2.0",
    "secp256k1": "^4.0.2",
    "tslib": "^1.10.0",
    "web3-eth-accounts": "^1.3.0"
  },
  "devDependencies": {
    "eth-gas-reporter": "^0.2.19",
    "mocha": "^8.1.3",
    "solidity-coverage": "^0.7.11",
    "truffle": "^5.1.52"
  }
}
